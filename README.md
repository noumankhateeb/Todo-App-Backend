To run the backend, just follow these steps:
1. **Install Dependencies:** Navigate to the backend directory and run:
   ```bash
   npm install
   ```
2. **Run the Project:** Start the server using:
   ```bash
   npm start
   ```

That's all! Your backend server will be up and running.